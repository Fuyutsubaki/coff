---
name: coff-issue-done
description: `issue/` の issue を完了扱いにする。受け入れ条件の充足と現状との整合を確認したうえで frontmatter の `status` を `done` に更新する。
---

<!--
完了マークを付ける専用 skill。done にする前に軽量ゲート（受け入れ条件の充足・現状との整合）で検証する。本文は変えない。状態の共通仕様は coff-detail-issue を唯一の参照元とする。
-->

## 手順

1. `.claude/skills/coff-detail-issue/SKILL.md` の状態仕様を把握する。 <!-- skill は自動では相互ロードされないので明示的に読む -->
2. 引数で渡された対象 issue ファイルを読む。
3. 完了検証（軽量ゲート）。通らなければ `done` にせず、理由を報告して差し戻す。
   - 受け入れ条件のチェックリストが全項目満たされているか確認する。未達があれば保留し、未達項目を挙げる。
   - 本文（現状・変更方針・実装詳細）と実リポジトリに明らかな食い違いが無いか確認する。あれば保留して指摘する。本文は書き換えない（矛盾の解消は、人が本文を直すかリスクへ移すかを判断する）。
   - 受け入れ条件が空・不在の issue は、その旨を警告したうえで検証を通す。
4. 検証を通ったら、frontmatter と `status` の有無で分岐して `done` にする。
   - frontmatter があり `status` があれば、その値を `done` に書き換える。
   - frontmatter はあるが `status` が無ければ、`status: done` を frontmatter に追加する。
   - frontmatter が無ければ、ファイル先頭に `---` / `status: done` / `---` を挿入する（本文はそのまま後続させる）。
5. 本文（`#` 以降）は一切変更しない。
6. 更新後のパスと新しい `status` を報告する。

## 適用範囲

- 更新は `done` 方向のみ。取り消し（`done` → `open`）は手編集で行う。
